package com.volvo.softproduct.canparser;

/**
 * Class used as a ENUM to describe if a signal is DOUBLE, LONG or FLAG
 */
public enum SignalType {
    DOUBLE,
    LONG,
    FLAG,
    NOVALUE
}